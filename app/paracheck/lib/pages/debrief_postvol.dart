import 'package:flutter/material.dart';

void main() {
  runApp(DebriefPostVolPage());
}

class DebriefPostVolPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Hello Worldddd'),
        ),
        body: Center(
          child: Text('Hello, Worldddd!'),
        ),
      ),
    );
  }
}